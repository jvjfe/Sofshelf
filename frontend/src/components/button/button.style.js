import { COLORS, FONT_SIZE } from "../../../src/constants/tema"

export const styles={
    btn: {
        width: "100%",
        height: 50,
        justifyContent: "center", 
        backgroundColor: COLORS.marromEscuro,
        borderRadius: 6,
        marginTop: 10
    },
    textos: {
        fontSize: FONT_SIZE.md,
        color: COLORS.branco,
        textAlign: "center"
    }
}